self.addEventListener("install", e => {
  e.waitUntil(
    caches.open("gym-pro").then(cache =>
      cache.addAll([
        "index.html",
        "styles.css",
        "app.js",
        "routines.js"
      ])
    )
  );
});
